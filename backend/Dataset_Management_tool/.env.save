xx# Local development database configuration
# Use SQLite by default to avoid missing PostgreSQL setup.
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/Database_management"
ENVIRONMENT=development
